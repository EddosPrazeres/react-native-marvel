import styled from 'styled-components/native'
import { colors, metrics } from '../../../styles'

export const ScrollView = styled.ScrollView`

`

export const ViewLists = styled.View`
  padding-top: ${metrics.baseSpacing(1)};
`